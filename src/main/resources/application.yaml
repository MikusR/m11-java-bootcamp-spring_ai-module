spring:
  application:
    name: springai-bootcamp-demo
  datasource:
    url: jdbc:sqlite:${SQLITE_DB_PATH:chatbot.db}
    driver-class-name: org.sqlite.JDBC
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.community.dialect.SQLiteDialect
    open-in-view: false
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: gemma3:270m
    openai:
      base-url: ${OPENROUTER_BASE_URL:https://openrouter.ai/api}
      api-key: ${OPENROUTER_API_KEY:}
      chat:
        options:
          model: ${OPENROUTER_MODEL:tencent/hy3:free}
          temperature: ${OPENROUTER_TEMPERATURE:0.9}
          max-tokens: ${OPENROUTER_MAX_COMPLETION_TOKENS:1024}
